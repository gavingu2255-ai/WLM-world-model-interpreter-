"""
lwm_extractor.py — Extract canonical features from parsed world‑model outputs.

This module converts the normalized LWM representation into canonical
feature primitives that the mapper can consume.
"""


def extract_features(parsed):
    """
    Extract canonical features from parsed LWM output.

    Parameters
    ----------
    parsed : dict
        Normalized world‑model output.

    Returns
    -------
    dict
        Canonicalized feature dictionary.
    """
    # MVP: pass-through
    return parsed
